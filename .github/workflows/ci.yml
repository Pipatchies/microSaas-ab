name: CI Pipeline

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  # 🧩 Backend (Django)
  backend:
    name: Backend - Django
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api/src
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with pylint
        run: pylint .

      - name: Format check with black
        run: black --check .

      - name: Security audit
        run: pip install safety && safety check

  # ⚛️ Frontend (Next.js)
  frontend:
    name: Frontend - Next.js
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: |
          corepack enable
          pnpm install

      - name: Lint
        run: pnpm lint

      - name: Prettier check
        run: pnpm prettier --check .

      - name: Build Next.js
        run: pnpm build

      - name: Audit dependencies
        run: pnpm audit --audit-level=moderate

  # 🐳 Docker build validation
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Dockerfiles
        run: |
          docker build -t tastyroad-backend -f api/Dockerfile .
          docker build -t tastyroad-frontend -f client/Dockerfile .
